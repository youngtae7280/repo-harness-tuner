interface:
  display_name: "Harness Tuner"
  short_description: "Read-only next-step and roadmap routing for repo-local Codex work."
  default_prompt: "Use $repo-harness-tuner to inspect this repo read-only and draft the next-step roadmap. Do not open apps, run tests, write files, or install anything; show preview/apply boundaries."

policy:
  allow_implicit_invocation: true
